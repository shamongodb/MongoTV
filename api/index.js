// Vercel serverless entry point. Vercel's @vercel/node runtime will invoke
// this file's default export as the request handler. An Express app is a
// valid handler because it implements (req, res) => void.
module.exports = require('../index.js');
